/**
 * Where the Donut Lab lives.
 *
 * It used to be `#donut-lab`, a full-screen overlay on the homepage. It is now
 * a page of its own — a second static HTML entry, see `vite.config.ts` — so
 * every "try the lab" call to action is an ordinary link and needs no click
 * handler. Kept as a constant because five components point at it.
 */
export const LAB_HREF = '/donut-lab/';
