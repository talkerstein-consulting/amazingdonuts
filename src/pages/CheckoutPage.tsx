import { useEffect, useRef, useState } from 'react';
import { ArrowLeft, Building2, CreditCard, LockKeyhole, MapPin, ShoppingBag, Store, Truck } from 'lucide-react';
import { ShopProvider, money, useShop } from '../lib/shop';
import AuthModal from '../shop/AuthModal';
import '../index.css';
import '../components/brand/brand.css';
import '../shop/shop.css';
import './commerce.css';

type Session = { user: null | { firstName: string; lastName: string; email: string }; profile: null | { default_phone?: string }; houseAccount: null | { id: string; organizationName: string; status: string; credit: { available: number } } };
type SquareCard = { attach: (selector: string) => Promise<void>; tokenize: (details: unknown) => Promise<{ status: string; token?: string; errors?: { message?: string }[] }>; destroy: () => Promise<boolean> };
declare global { interface Window { Square?: { payments: (appId: string, locationId: string) => { card: () => Promise<SquareCard> } } } }

const tomorrow = () => { const d = new Date(Date.now() + 86400000); d.setHours(9,0,0,0); return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,16); };
async function api(path:string,options?:RequestInit){const r=await fetch(`/api/house${path}`,{headers:{'Content-Type':'application/json',...(options?.headers||{})},...options});const b=await r.json();if(!r.ok)throw new Error(b?.error?.message||'Request failed.');return b;}

function Checkout(){
  const {lines,subtotal,clear}=useShop(),[session,setSession]=useState<Session>(),[authOpen,setAuthOpen]=useState(false),[method,setMethod]=useState<'card'|'house_account'>('card'),[fulfillment,setFulfillment]=useState<'pickup'|'delivery'>('pickup'),[scheduledAt,setScheduledAt]=useState(tomorrow),[phone,setPhone]=useState(''),[address,setAddress]=useState({addressLine1:'',addressLine2:'',locality:'Toronto',administrativeDistrictLevel1:'ON',postalCode:'',country:'CA'}),[config,setConfig]=useState<any>(),[error,setError]=useState(''),[busy,setBusy]=useState(false),[success,setSuccess]=useState<any>();
  const card=useRef<SquareCard | undefined>(undefined);
  const loadSession=()=>api('/storefront/session').then(body=>{setSession(body);setPhone(body.profile?.default_phone||'');if(!body.user)setAuthOpen(true)}).catch(cause=>setError(cause.message));
  useEffect(()=>{loadSession();api('/storefront/config').then(setConfig).catch(cause=>setError(cause.message));},[]);
  useEffect(()=>{if(!config?.applicationId||method!=="card"||!session?.user)return;let cancelled=false;const mount=async()=>{if(!window.Square){const script=document.createElement('script');script.src=config.environment==='sandbox'?'https://sandbox.web.squarecdn.com/v1/square.js':'https://web.squarecdn.com/v1/square.js';script.async=true;await new Promise<void>((resolve,reject)=>{script.onload=()=>resolve();script.onerror=()=>reject(new Error('Secure card fields could not load.'));document.head.appendChild(script);});}if(cancelled||!window.Square)return;card.current=await (await window.Square.payments(config.applicationId,config.locationId).card());await card.current.attach('#square-card');};mount().catch(cause=>setError(cause.message));return()=>{cancelled=true;card.current?.destroy().catch(()=>{});card.current=undefined};},[config,method,session?.user]);
  const place=async(e:React.FormEvent)=>{e.preventDefault();if(!session?.user){setAuthOpen(true);return}if(!lines.length)return;setBusy(true);setError('');try{const fulfillmentBody={type:fulfillment,scheduledAt:new Date(scheduledAt).toISOString(),recipient:{displayName:`${session.user.firstName} ${session.user.lastName}`,email:session.user.email,phone},...(fulfillment==='delivery'?{address}: {})},items=lines.map(x=>({name:x.product.name,quantity:x.qty}));const quote=(await api('/storefront/quote',{method:'POST',body:JSON.stringify({items,fulfillment:fulfillmentBody})})).order;let sourceId: string|undefined;if(method==='card'){if(!card.current)throw new Error('The secure card form is still loading.');const token=await card.current.tokenize({amount:(quote.total/100).toFixed(2),currencyCode:quote.currency,intent:'CHARGE',customerInitiated:true,sellerKeyedIn:false,billingContact:{givenName:session.user.firstName,familyName:session.user.lastName,email:session.user.email,phone,addressLines:[address.addressLine1,address.addressLine2].filter(Boolean),city:address.locality,state:address.administrativeDistrictLevel1,postalCode:address.postalCode,countryCode:address.country}});if(token.status!=='OK'||!token.token)throw new Error(token.errors?.[0]?.message||'Card authorization failed.');sourceId=token.token;}const result=await api('/storefront/checkout',{method:'POST',body:JSON.stringify({idempotencyKey:crypto.randomUUID(),items,fulfillment:fulfillmentBody,paymentMethod:method,sourceId})});clear();setSuccess(result.order);}catch(cause){setError(cause instanceof Error?cause.message:'Checkout failed.');}finally{setBusy(false)}};
  if(success)return <main className="commerce-shell"><section className="commerce-success"><ShoppingBag/><p>Order confirmed</p><h1>Your box is on the bakery’s list.</h1><strong>Order #{success.id.slice(-8)}</strong><span>{money(success.total/100)} · {fulfillment}</span><a href="/account/">View my orders</a></section></main>;
  return <main className="commerce-shell"><header className="commerce-top"><a href="/shop/"><ArrowLeft/> Back to the shop</a><img src="/img/logo-amazing-donuts.svg" alt="Amazing Donuts"/><a href="/account/">My account</a></header><div className="checkout-grid"><form className="checkout-form" onSubmit={place}><div className="commerce-heading"><p>Secure checkout</p><h1>Finish your order</h1><span><LockKeyhole/> Sign-in is required. Guest checkout is not available.</span></div>{session?.user?<section className="signed-row"><div><strong>{session.user.firstName} {session.user.lastName}</strong><span>{session.user.email}</span></div><a href="/account/">Manage account</a></section>:<section className="signin-gate"><h2>Sign in to continue</h2><p>Your cart is saved while you sign in or create an account.</p><button type="button" onClick={()=>setAuthOpen(true)}>Sign in or create account</button></section>}<fieldset disabled={!session?.user||busy}><legend>Fulfillment</legend><div className="segment"><button type="button" className={fulfillment==='pickup'?'active':''} onClick={()=>setFulfillment('pickup')}><Store/> Pickup</button><button type="button" className={fulfillment==='delivery'?'active':''} onClick={()=>setFulfillment('delivery')}><Truck/> Delivery</button></div><label><span>Date and time</span><input type="datetime-local" min={tomorrow()} value={scheduledAt} onChange={e=>setScheduledAt(e.target.value)} required/></label><label><span>Phone</span><input type="tel" value={phone} onChange={e=>setPhone(e.target.value)} required/></label>{fulfillment==='delivery'&&<div className="address-fields"><label><span>Street address</span><input value={address.addressLine1} onChange={e=>setAddress({...address,addressLine1:e.target.value})} required/></label><label><span>Unit</span><input value={address.addressLine2} onChange={e=>setAddress({...address,addressLine2:e.target.value})}/></label><label><span>City</span><input value={address.locality} onChange={e=>setAddress({...address,locality:e.target.value})} required/></label><label><span>Postal code</span><input value={address.postalCode} onChange={e=>setAddress({...address,postalCode:e.target.value})} required/></label></div>}</fieldset><fieldset disabled={!session?.user||busy}><legend>Payment</legend><div className="payment-options"><button type="button" className={method==='card'?'active':''} onClick={()=>setMethod('card')}><CreditCard/><span><strong>Card</strong><small>Securely processed by Square</small></span></button>{session?.houseAccount?.status==='active'&&<button type="button" className={method==='house_account'?'active':''} onClick={()=>setMethod('house_account')}><Building2/><span><strong>Charge house account</strong><small>{session.houseAccount.organizationName} · {money(session.houseAccount.credit.available/100)} available</small></span></button>}</div>{method==='card'&&<div id="square-card" className="square-card"/>}{method==='house_account'&&<p className="house-note">This order will appear in Square and on your next house-account statement.</p>}</fieldset>{error&&<p className="checkout-error" role="alert">{error}</p>}<button className="place-order" disabled={!session?.user||!lines.length||busy}>{busy?'Placing order...':`Place order · ${money(subtotal)}`}</button></form><aside className="order-summary"><p>Your box</p><h2>{lines.reduce((n,x)=>n+x.qty,0)} items</h2>{lines.map(x=><div className="summary-line" key={x.product.id}><img src={x.product.img} alt=""/><span><strong>{x.product.name}</strong><small>Qty {x.qty}</small></span><b>{money(Number(x.product.price.replace(/[^0-9.]/g,''))*x.qty)}</b></div>)}<div className="summary-total"><span>Estimated subtotal</span><strong>{money(subtotal)}</strong></div><small>Square confirms catalog pricing, HST, discounts, and the final total before payment.</small></aside></div><AuthModal open={authOpen} onClose={()=>setAuthOpen(false)} onSuccess={loadSession}/></main>;
}
export default function CheckoutPage(){return <ShopProvider><Checkout/></ShopProvider>}
