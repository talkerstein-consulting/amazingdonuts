import { useEffect, useState } from 'react';
import { ArrowLeft, Building2, LogOut, Package, ReceiptText, UserRound } from 'lucide-react';
import AuthModal from '../shop/AuthModal';
import '../index.css';import '../components/brand/brand.css';import '../shop/shop.css';import './commerce.css';
const cash=(n:number,c='CAD')=>new Intl.NumberFormat('en-CA',{style:'currency',currency:c}).format(Number(n||0)/100),day=(v:string)=>new Date(v).toLocaleDateString('en-CA',{year:'numeric',month:'short',day:'numeric'});
async function api(path:string,options?:RequestInit){const r=await fetch(`/api/house${path}`,{headers:{'Content-Type':'application/json',...(options?.headers||{})},...options});const b=r.status===204?null:await r.json();if(!r.ok)throw new Error(b?.error?.message||'Request failed.');return b;}
export default function AccountPage(){const [session,setSession]=useState<any>(),[orders,setOrders]=useState<any[]>([]),[authOpen,setAuthOpen]=useState(false),[view,setView]=useState<'orders'|'profile'>('orders'),[message,setMessage]=useState('');const load=()=>api('/storefront/session').then(async s=>{setSession(s);if(s.user)setOrders((await api('/storefront/orders')).orders);else setAuthOpen(true)}).catch(e=>setMessage(e.message));useEffect(()=>{void load()},[]);if(!session?.user)return <main className="commerce-shell account-gate"><a href="/shop/"><ArrowLeft/> Back to shop</a><section><UserRound/><h1>Your Amazing Donuts account</h1><p>Sign in to see orders, manage your details, and access approved house-account credit.</p><button onClick={()=>setAuthOpen(true)}>Sign in or create account</button></section><AuthModal open={authOpen} onClose={()=>setAuthOpen(false)} onSuccess={load}/></main>;return <main className="commerce-shell"><header className="commerce-top"><a href="/shop/"><ArrowLeft/> Shop</a><img src="/img/logo-amazing-donuts.svg" alt="Amazing Donuts"/><button onClick={async()=>{await api('/auth/logout',{method:'POST'});location.assign('/shop/')}}><LogOut/> Sign out</button></header><div className="account-layout"><aside className="account-nav"><div className="account-person"><span>{session.user.firstName[0]}{session.user.lastName[0]}</span><strong>{session.user.firstName} {session.user.lastName}</strong><small>{session.user.email}</small></div><button className={view==='orders'?'active':''} onClick={()=>setView('orders')}><Package/> Orders</button><button className={view==='profile'?'active':''} onClick={()=>setView('profile')}><UserRound/> Profile</button>{session.houseAccount&&<div className="house-credit"><Building2/><strong>{session.houseAccount.organizationName}</strong><span>{cash(session.houseAccount.credit.available)} available</span></div>}</aside><section className="account-content">{view==='orders'?<><div className="commerce-heading"><p>Order history</p><h1>Your orders</h1></div>{orders.length?<div className="customer-orders">{orders.map(order=><article key={order.id}><header><div><span>{day(order.ordered_at)}</span><strong>Order #{order.square_order_id.slice(-8)}</strong></div><em>{order.status}</em></header><div>{(order.line_items||[]).map((line:any)=><p key={line.uid||line.name}><span>{line.name} × {line.quantity}</span><b>{cash(line.total_money?.amount,order.currency)}</b></p>)}</div><footer><span>{order.payment_method==='house_account'?'House account':'Card'} · {order.fulfillment?.type}</span><strong>{cash(order.total,order.currency)}</strong></footer></article>)}</div>:<div className="no-orders"><ReceiptText/><h2>No orders yet</h2><p>Your completed website orders will appear here.</p><a href="/shop/">Start an order</a></div>}</>:<Profile session={session} onSaved={()=>{setMessage('Profile updated.');load()}}/>}{message&&<p className="account-message">{message}</p>}</section></div></main>}
function Profile({session,onSaved}:{session:any;onSaved:()=>void}){const p=session.profile||{};return <><div className="commerce-heading"><p>Account details</p><h1>Your profile</h1></div><form className="profile-form" onSubmit={async e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.currentTarget));await api('/storefront/profile',{method:'PATCH',body:JSON.stringify({firstName:d.firstName,lastName:d.lastName,phone:d.phone,address:{addressLine1:d.addressLine1,addressLine2:d.addressLine2,locality:d.locality,administrativeDistrictLevel1:'ON',postalCode:d.postalCode,country:'CA'}})});onSaved()}}><label><span>First name</span><input name="firstName" defaultValue={p.first_name} required/></label><label><span>Last name</span><input name="lastName" defaultValue={p.last_name} required/></label><label><span>Email</span><input value={p.email} disabled/></label><label><span>Phone</span><input name="phone" defaultValue={p.default_phone||p.phone||''}/></label><label className="wide"><span>Street address</span><input name="addressLine1" defaultValue={p.default_address?.addressLine1||''}/></label><label><span>Unit</span><input name="addressLine2" defaultValue={p.default_address?.addressLine2||''}/></label><label><span>City</span><input name="locality" defaultValue={p.default_address?.locality||'Toronto'}/></label><label><span>Postal code</span><input name="postalCode" defaultValue={p.default_address?.postalCode||''}/></label><button>Save profile</button></form></>}
